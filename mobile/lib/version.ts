// JS bundle version — update this with every OTA update.
// Unlike Constants.expoConfig.version (baked into the native binary),
// this value is part of the JS bundle and reflects the latest OTA update.
export const JS_VERSION = "1.3.3";
